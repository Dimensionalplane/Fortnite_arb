# DEPLOY

## Prerequisites
- Python 3.8+
- `pip` for dependency management.

## Setup Instructions
1. Clone the repository.
2. Install dependencies (once `requirements.txt` is created): `pip install -r requirements.txt`.
3. Run the application: `python src/scanner.py`.

## Environment Variables
- `STEAM_API_KEY`: Required for fetching data from the Steam Market.

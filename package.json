{
  "name": "digital-asset-arbitrage",
  "version": "1.5.0",
  "scripts": {
    "start:backend": "go run backend/main.go",
    "start:frontend": "cd frontend && npm run dev",
    "update-version": "node -e \"const fs = require('fs'); const pkg = JSON.parse(fs.readFileSync('package.json')); fs.writeFileSync('VERSION.md', pkg.version + '\\n');\""
  }
}
